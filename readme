本系统在linux系统下完成，查询结果直接显示到终端上，如果需要查询详细的信息，可以重定向到文件

结构安排：
	get_json.h get_json.c
	发送请求到豆瓣网上，并且获取返回的信息，存放在buf里面

	json.h	json.c
	获取buf里面的内容，递归解析，并且将信息存放在结构体struct arr内

	print_book.h print_book.c
	递归打印struct里面的内容

	main.c
	组织结构

	Makefile
	工程文件

编译：
	make
	
运行：
	./main

		